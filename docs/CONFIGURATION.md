# unified-domain-client — Configuration

## Config

Configuration via environment variables or config classes. See README.md and package source for fields.

## Secrets

No hardcoded secrets. Use Secret Manager or env vars.
