name: Quality Gates

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality-gates:
    uses: IggyIkenna/unified-trading-pm/.github/workflows/python-quality-gates.yml@live-defi-rollout
    with:
      dep_repos: "unified-trading-library unified-config-interface unified-cloud-interface unified-api-contracts unified-internal-contracts unified-ml-interface"
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}
